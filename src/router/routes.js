import { routes as modulesRotes } from '../modules'

export default [ ...modulesRotes ]

/*
const rootRotes = [
    { path: '/', redirect: '/home' }
]

export default [ ...rootRotes, ...modulesRotes ]
*/
